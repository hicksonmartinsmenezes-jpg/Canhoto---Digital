export default function SetoresPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Setores</h1>
      <p className="mt-1 text-sm text-zinc-500">
        Departamentos da Rildon envolvidos no controle de canhotos.
      </p>
      <div className="mt-8 rounded-2xl border border-zinc-200 bg-white p-6 text-sm text-zinc-500">
        Página em construção — lista + cadastro (tabela `setores`).
      </div>
    </div>
  );
}
