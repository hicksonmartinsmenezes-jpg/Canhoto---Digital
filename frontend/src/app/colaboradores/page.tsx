export default function ColaboradoresPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Colaboradores</h1>
      <p className="mt-1 text-sm text-zinc-500">
        Pessoas da empresa — quem recebe/assina canhotos e/ou tem acesso ao
        sistema (papel: admin, gestor de setor, colaborador).
      </p>
      <div className="mt-8 rounded-2xl border border-zinc-200 bg-white p-6 text-sm text-zinc-500">
        Página em construção — lista + cadastro (tabela `colaboradores`).
      </div>
    </div>
  );
}
