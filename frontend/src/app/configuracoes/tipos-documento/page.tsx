export default function TiposDocumentoPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">
        Tipos de Documento
      </h1>
      <p className="mt-1 text-sm text-zinc-500">
        Categorias de documento aceitas nos canhotos (Nota Fiscal, Contrato,
        Memorando, Ordem de Serviço, Requisição, Recibo, Outro).
      </p>
      <div className="mt-8 rounded-2xl border border-zinc-200 bg-white p-6 text-sm text-zinc-500">
        Página em construção — lista + cadastro (tabela `tipos_documento`).
      </div>
    </div>
  );
}
